const regex={email:/^[^\s@]+@[^\s@]+\.[^\s@]+$/,cpf:/^\d{3}\.\d{3}\.\d{3}-\d{2}$/,telefone:/^\(\d{2}\) \d{5}-\d{4}$/,cep:/^\d{5}-\d{3}$/};
function erro(campo,msg){const el=document.getElementById(campo);const out=document.getElementById(`erro-${campo}`);el.classList.toggle('invalido',!!msg);el.classList.toggle('valido',!msg);out.textContent=msg||'';el.setAttribute('aria-invalid',String(!!msg));if(msg)el.setAttribute('aria-describedby',`erro-${campo}`);else el.removeAttribute('aria-describedby');return !msg}
export function validarFormulario(d){let ok=true;ok=erro('nome',d.nome.trim().length<3?'Informe seu nome completo.':'')&&ok;ok=erro('email',!regex.email.test(d.email)?'Informe um e-mail válido.':'')&&ok;ok=erro('cpf',!regex.cpf.test(d.cpf)?'Use o formato 000.000.000-00.':'')&&ok;ok=erro('telefone',!regex.telefone.test(d.telefone)?'Use o formato (11) 99999-9999.':'')&&ok;ok=erro('cep',!regex.cep.test(d.cep)?'Use o formato 00000-000.':'')&&ok;ok=erro('participacao',!d.participacao?'Selecione uma opção.':'')&&ok;return ok}
export const mascaras={cpf:v=>v.replace(/\D/g,'').slice(0,11).replace(/(\d{3})(\d)/,'$1.$2').replace(/(\d{3})(\d)/,'$1.$2').replace(/(\d{3})(\d{1,2})$/,'$1-$2'),telefone:v=>v.replace(/\D/g,'').slice(0,11).replace(/^(\d{2})(\d)/,'($1) $2').replace(/(\d{5})(\d)/,'$1-$2'),cep:v=>v.replace(/\D/g,'').slice(0,8).replace(/(\d{5})(\d)/,'$1-$2')};
